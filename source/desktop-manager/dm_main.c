#include <basics.h>

/**
 * @attention Don't rename this function, if you wanted to rename it, u must change the linker also.
 * 
 */
void dw_main(){
    int i = 10;
    i = 110;
    return; // Safe to do.
}